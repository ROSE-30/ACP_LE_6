View
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>All Ratings</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        a {
            text-decoration: none;
            color: blue;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<h3>All Ratings</h3>

<p><a href="{{ url_for('rate') }}">Add Rating</a></p>

<table>
    <tr>
        <th>Name</th>
        <th>Movie</th>
        <th>Rating</th>
        <th>Remarks</th>
        <th>Created At</th>
        <th>Actions</th>
    </tr>

    {% for rating in ratings %}
    <tr>
        <td>{{ rating.name }}</td>
        <td>{{ rating.movie }}</td>
        <td>{{ rating.stars }}</td>
        <td>{{ rating.remarks }}</td>
        <td>{{ rating.created_at.strftime('%Y-%m-%d %H:%M') }}</td>
        <td>
            <a href="{{ url_for('edit_rating', id=rating.id) }}">Edit</a> | 
            <a href="{{ url_for('delete_rating', id=rating.id) }}" 
               onclick="return confirm('Are you sure you want to delete this rating?');">
               Delete
            </a>
        </td>
    </tr>
    {% else %}
    <tr>
        <td colspan="6">No ratings available.</td>
    </tr>
    {% endfor %}
</table>

<p><a href="{{ url_for('logout') }}">Logout</a></p>

</body>
</html>

